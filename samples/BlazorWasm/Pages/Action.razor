@page "/action/{text}"
@page "/account/{text}"

This link is just to demonstrate the App Drawer's scrollability when there are a lot of action items.

@code {
    [Parameter] public string text { get; set; }
}